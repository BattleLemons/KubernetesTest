apiVersion: v1
kind: Secret
metadata:
  name: {{ include "lampstack.fullname" . }}-mysql-creds
type: Opaque
stringData:
  mysql-root-password: {{ .Values.mysql.rootPassword | quote }}
  mysql-database:      {{ .Values.mysql.database | quote }}
